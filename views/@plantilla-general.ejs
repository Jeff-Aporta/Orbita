<!DOCTYPE html>
<html lang="en">
<head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Orbita</title>

        <%- include(info_pagina.subir_a_raiz + '@headerlinks') %>

        <% if(user){ %>
                <script>
                        location.href = '/login/';
                </script>
        <% } %>

        <script src="<%- info_pagina.carpeta %><%- info_pagina.nombre  %><%- !user && info_pagina.JSXUnUser ? "!" : "" %>.jsx" type="text/babel" defer></script>
        
        <script src="/JSX/!fin.jsx" type="text/babel" defer></script>
</head>
<body>
</body>
</html>